import { LightningElement } from 'lwc';
import AlertMessage from '@salesforce/label/c.L_PR_AlertMessage';
export default class L_PR_showAlertBanner extends LightningElement {
    label = {
        AlertMessage
    };
}